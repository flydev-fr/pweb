// The module half of the accept corpus: `type="module"` WITH a
// same-origin src is exactly what Vite's real output emits, and it is
// accepted for the same reason a classic same-origin script is -
// script-src 'self' runs it.
export const started = true;
