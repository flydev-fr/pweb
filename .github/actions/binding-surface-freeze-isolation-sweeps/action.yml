name: 'Binding surface + freeze-isolation sweeps'
description: 'CAP-11A: the body of the "Binding surface + freeze-isolation sweeps" step of the one platform-leg sequence.'
runs:
  using: composite
  steps:
    - name: windows
      shell: pwsh
      run: pwsh -NoProfile -File test/core/check_binding_surface.ps1
