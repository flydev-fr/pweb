name: 'CAP-7M0 per-arch headline facts to the run summary'
description: 'CAP-11A: the body of the "CAP-7M0 per-arch headline facts to the run summary" step of the one platform-leg sequence.'
runs:
  using: composite
  steps:
    - name: macos-x64, macos-arm64
      shell: bash
      run: test/cap7m/summarize_cap7m.sh
