name: 'CAP-7L paired ABI probes + hand-declared symbol presence gate'
description: 'CAP-11A: the body of the "CAP-7L paired ABI probes + hand-declared symbol presence gate" step of the one platform-leg sequence.'
runs:
  using: composite
  steps:
    - name: linux
      shell: bash
      run: test/cap7l/check_abi.sh
