name: 'CAP-10B1 create gates + evidence'
description: 'CAP-11A: the body of the "CAP-10B1 create gates + evidence" step of the one platform-leg sequence.'
inputs:
  target:
    description: 'windows | linux | macos-x64 | macos-arm64'
    required: true
runs:
  using: composite
  steps:
    - name: windows
      if: ${{ inputs.target == 'windows' }}
      shell: pwsh
      run: |
        pwsh -NoProfile -File test/cap10b1/run_cap10b1_gates.ps1 2>&1 |
          Tee-Object -FilePath build/cap10b1-gates.log
        if ($LASTEXITCODE -ne 0) { throw 'CAP-10B1 create gates FAILED' }
    - name: linux, macos-x64, macos-arm64
      if: ${{ inputs.target != 'windows' }}
      shell: pwsh
      run: |
        pwsh -NoProfile -File test/cap10b1/run_cap10b1_gates.ps1
        if ($LASTEXITCODE -ne 0) { throw 'CAP-10B1 create gates FAILED' }
