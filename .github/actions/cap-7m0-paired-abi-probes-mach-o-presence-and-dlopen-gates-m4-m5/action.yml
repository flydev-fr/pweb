name: 'CAP-7M0 paired ABI probes + Mach-O presence and dlopen gates (M4/M5)'
description: 'CAP-11A: the body of the "CAP-7M0 paired ABI probes + Mach-O presence and dlopen gates (M4/M5)" step of the one platform-leg sequence.'
runs:
  using: composite
  steps:
    - name: macos-x64, macos-arm64
      shell: bash
      # No --clean: fresh workspace (see the dylib build step).
      run: test/cap7m/check_abi.sh
