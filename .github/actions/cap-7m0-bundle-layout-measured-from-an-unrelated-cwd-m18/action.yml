name: 'CAP-7M0 bundle layout measured from an unrelated CWD (M18)'
description: 'CAP-11A: the body of the "CAP-7M0 bundle layout measured from an unrelated CWD (M18)" step of the one platform-leg sequence.'
runs:
  using: composite
  steps:
    - name: macos-x64, macos-arm64
      shell: bash
      run: test/cap7m/check_release_layout.sh
